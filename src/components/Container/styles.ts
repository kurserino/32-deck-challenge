import * as stylex from '@stylexjs/stylex';

export const styles = stylex.create({
  wrapper: {
    backgroundSize: 'cover',
    width: '100%',
    maxWidth: 1280,
    margin: '0 auto',
    padding: '0 20px',
  },
});
